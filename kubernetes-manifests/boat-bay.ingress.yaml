apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: boat-bay-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
  namespace: dope
spec:
  rules:
    - host: "boat-bay.proto.backbasecloud.com"
      http:
        paths:
          - backend:
              serviceName: boat-bay
              servicePort: http
            path: /

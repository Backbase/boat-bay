apiVersion: v1
kind: Service
metadata:
  name: boat-bay-server
  labels:
    app: boat-bay-server
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: boat-bay-server
    tier: backend
  ports:
    - port: 8080
      targetPort: http
